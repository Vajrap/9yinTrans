/*
This function use for the current text file that was not yet fully translated.
When recieved one text file, the function returns two text files.
- first file contains all translated lines in the former file.
- second file contains all untranslated lines in the former file.
- This means the function will 'separate' lines that are translated from that which is not translated
- user must then, put the untranslated lines after the translated lines and start the translation.

### Example
*/

class Separator {
    constructor(){

    }


}